package C8_String2;import java.util.Arrays;/** * Created by HaoYu on 2017/10/19. */public class ReOrderArray {    public int[] reorder(int[] array) {        // Write your solution here.        if (array.length % 2 == 0) {            reorder(array, 0, array.length - 1);        } else {            reorder(array, 0, array.length - 2);        }        // TODO 奇数情况最后一个不计        return array;    }    private void reorder(int[] array, int l, int r) {        if (r - l <= 1) {            return;        }        int size = r - l + 1;        int mid = l + size / 2;        int lmid = l + size / 4;        int rmid = l + 3 * size / 4;        reverse(array, lmid, mid - 1);//TODO 都是到-1        reverse(array, mid, rmid - 1);        reverse(array, lmid, rmid - 1);        reorder(array, l, l + 2* (lmid - l) - 1);        reorder(array, l + 2* (lmid - l), r);    }    private void reverse(int[] array, int left, int right) {        while (left < right) {            int tmp = array[left];            array[left] = array[right];            array[right] = tmp;            left++;            right--;        }    }    public static void main(String[] args){        System.out.print(Arrays.toString(new ReOrderArray().reorder(new int[] {1, 2, 3, 4, 5, 6, 7, 8})));    }}